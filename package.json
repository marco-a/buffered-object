{
  "name": "buffered-object",
  "url": "https://github.com/marco-a/buffered-object",
  "version": "2.0.0",
  "description": "A small class to enable buffering WebSocket (or similar) packets!",
  "main": "./es5/BufferedObject.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "./compile.sh"
  },
  "author": "Marco Agnoli",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-preset-es2015": "^6.22.0"
  }
}
